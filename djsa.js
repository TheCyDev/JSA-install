// Start //
const Discord = require('discord.js');
const loging = console.log; // console

const instl = require; // installing
// {
// let appis;
		//	const apis = ["https://vacefron.nl/api/", "https://api.alexflipnote.dev/"];
			
		//	const aps = Math.floor(Math.random() * apis.length);
		//	const jons = apis[aps].join(" / ");
			
	//	message.channel.send(`\`${jons}\``);
// }

const timeout = setTimeout;

				const interval = setInterval;
				
		// const start = module.exports;
		
				// end //